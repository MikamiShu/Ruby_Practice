<h1>UsersController#new</h1>

<%= form_with model: @user do |form| %> 
    <div><%= form.label :name %> <%= form.text_field :name %></div>
    <div><%= form.label :email %> <%= form.text_area :email %></div>
    <div><%= button_to "作成", user_path(form) %></div>
<% end %>