<h1>new_test</h1>

<%= form_with model: @user do |form| %> 
    <div><%= form.label :title %> <%= form.text_field :title %></div>
    <div><%= form.label :text_content %> <%= form.text_area :text_content %></div>
    <div><%= form.submit "投稿" %></div>
<% end %>